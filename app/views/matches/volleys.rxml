xml.instruct!
xml.data do
  @match.volleys.each do |volley|
	  xml.event(commit_messages(volley), :start => volley.created_at.strftime("%B %d %Y %H:%M:%S %Z"), :end => volley.updated_at.strftime("%B %d %Y %H:%M:%S %Z"), :isDuration => true, :title => volley.player.display_name ) 
  end
end