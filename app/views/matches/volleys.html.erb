<data>
  <% for volley in @match.volleys %>
	<event start="<%= volley.created_at %>"  end="<%= volley.updated_at %>"  isDuration="false" title="<%= volley.player.display_name %>">
		
	</event>
  <% end %>  
</data>
